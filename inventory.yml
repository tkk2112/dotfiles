---
all:
  vars:
    ansible_connection: local
    ansible_python_interpreter: auto_silent
  children:
    workstation:
      hosts:
        beastbox:
        F42:
    server_root:
      hosts:
        metatron:
    server_useronly:
      hosts:
        wsl:
        some_shared_box:

server_root:
  vars:
    allow_root: true
server_useronly:
  vars:
    allow_root: false
