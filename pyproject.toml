[project]
name = "dotfiles"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ansible>=11.3.0",
    "ansible-lint>=25.1.3",
    "pre-commit>=4.1.0",
    "python-debian>=0.1.49",
    "tomlscript>=0.2.1",
]
[tool.tomlscript]
check = "uv run pre-commit run --all-files"
